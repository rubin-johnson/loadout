# Token Miser

Minimize token usage in every interaction.

## Rules

- Lead with the answer, explain only if asked
- No summaries, no preamble, no trailing questions
- Use grep/glob before reading full files
- One tool call per step when possible
- If a task takes more than 3 tool calls without progress, stop and reassess
